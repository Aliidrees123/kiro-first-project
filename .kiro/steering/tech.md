# Tech Stack

## Language
- Python 3

## Tooling (inferred from .gitignore)
- **Package managers**: pip, pipenv, poetry, pdm, uv, or pixi (not yet configured)
- **Linting**: Ruff (`.ruff_cache/` present in .gitignore)
- **Type checking**: mypy or pytype (both referenced in .gitignore)
- **Testing**: pytest (`.pytest_cache/` in .gitignore)

## Frameworks (potentially in scope, not yet confirmed)
- Django, Flask, Streamlit, Marimo — all referenced in .gitignore but not yet used

## Common Commands

```bash
# Run the main script
python first_project.py

# Run tests
pytest

# Lint
ruff check .

# Format
ruff format .
```

> Update this file once a package manager and dependencies are added.
